🛡️ Private Family Budget (Tauri Edition)

Це десктопний додаток для ведення сімейного бюджету з акцентом на максимальну приватність.
Додаток працює локально, не відправляє дані на зовнішні сервери та дозволяє аналізувати фінанси на основі банківських виписок.

🚀 Основна філософія

Local-first: Всі дані зберігаються у локальному файлі (JSON) на комп'ютері користувача.

Privacy: Жодної аналітики, трекерів чи хмарної синхронізації.

Automation: Імпорт CSV (Commbank) та автоматична категоризація за ключовими словами.

🛠️ Технічний стек

Core: Tauri v2 (Rust) — для створення легкого та безпечного вікна.

Frontend: React — інтерфейс.

Styling: Bulma CSS — стилізація.

Charts: Recharts — візуалізація даних.

Icons: Lucide React — іконки.

📦 Встановлення та Запуск

Передумови

Кроки для розробки

Встановлення залежностей:

npm install

Запуск у режимі розробки:

npm run tauri dev

Це запустить React-сервер та відкриє вікно Tauri.

Створення релізної збірки:

npm run tauri build

Інсталятор з'явиться у папці src-tauri/target/release/bundle.

📂 Структура проекту

/src - React код (інтерфейс, логіка категоризації).

/src-tauri - Rust код (конфігурація вікна, системні дозволи).

/src/components - UI компоненти (Dashboard, Import, Settings).

/src/lib - Утиліти (парсинг CSV, логіка правил).

📝 Формати даних

Програма очікує CSV файл, де роздільником є кома або крапка з комою.
