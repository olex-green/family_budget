Технічне завдання: Модуль Локального ШІ (Rust + Burn)

🎯 Мета

Реалізувати швидкий, офлайновий та приватний механізм категоризації фінансових транзакцій у середовищі Tauri, використовуючи Rust-фреймворк глибокого навчання Burn. Відмовитись від зовнішніх HTTP-сервісів (Ollama/OpenAI) на користь вбудованої логіки векторної подібності (Embedding Similarity).

🧠 Концепція: Zero-Shot Classification через Embeddings

Замість генерації тексту (LLM), ми використовуємо порівняння семантичних векторів.

Тексти категорій (наприклад, "Їжа", "Транспорт") перетворюються на вектори (масиви чисел) при запуску.

Опис транзакції перетворюється на вектор.

Категорія обирається за найменшою відстанню (Cosine Similarity) між вектором транзакції та векторами категорій.

🛠️ Технічні Вимоги

1. Стек технологій

Фреймворк: Burn (Rust).

Бекенд: wgpu (для кросплатформного GPU прискорення) або ndarray (для CPU fallback).

Токенізація: tokenizers (HuggingFace).

Модель: all-MiniLM-L6-v2 (або аналог nomic-embed-text). Модель має бути експортована у формат, сумісний з Burn.

2. Функціональні блоку

A. Ініціалізація (SemanticClassifier::new)

Завантажити ваги моделі та конфігурацію токенізатора з локальних ассетів (resources/).

Прийняти список поточних категорій користувача.

Згенерувати та закешувати ембедінги для кожної категорії (Pre-computed embeddings), щоб не перераховувати їх для кожної транзакції.

B. Обробка (SemanticClassifier::classify)

Вхід: description: String (опис транзакції).

Дії:

Токенізувати вхідний рядок.

Прогнати через модель (Inference) для отримання вектору.

Розрахувати Cosine Similarity з усіма векторами категорій.

Знайти max(similarity).

Вихід: Назва найкращої категорії та score впевненості (0.0 - 1.0).

Threshold: Якщо впевненість менша за 0.4 (поріг налаштовується), повертати "Інше".

C. Інтеграція з Tauri

Модель має жити в AppState (використовуючи Mutex або RwLock), ініціалізуватися асинхронно в окремому потоці, щоб не блокувати запуск UI.

Команда classify_transaction має обробляти помилку "Model Loading", якщо користувач спробував класифікувати дані до завершення ініціалізації.

📂 Структура файлів (пропозиція)

src-tauri/
├── src/
│ ├── ai/
│ │ ├── mod.rs // Публічний інтерфейс
│ │ ├── model.rs // Визначення архітектури Burn (якщо не використовуємо готовий crate)
│ │ └── classifier.rs // Логіка Cosine Similarity
│ ├── commands.rs // Tauri команди
│ └── main.rs // Реєстрація State
├── assets/
│ ├── model/
│ │ ├── model.mpk // Ваги моделі (Burn format)
│ │ └── tokenizer.json // Конфіг токенізатора

✅ Критерії Прийомки (Definition of Done)

Автономність: Додаток працює без інтернету. Ваги моделі "зашиті" в інсталятор або завантажуються один раз.

Продуктивність: Класифікація однієї транзакції займає менше 50мс на середньому CPU.

Точність:

"Uber Trip" -> "Транспорт" (при наявності такої категорії).

"Сільпо" -> "Продукти".

"Зарплата" -> "Дохід" (або ігнорування, якщо це витрата).

Стабільність: Додаток не падає (panic), якщо опис транзакції містить спецсимволи або порожній.

📝 Примітки для розробки

Для початку можна використати крейт bert-burn або приклади з репозиторію Burn для завантаження архітектури BERT.

Увага на розмір бінарного файлу: модель додасть ~90МБ.
